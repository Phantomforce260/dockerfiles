networks:
    web:
        external: true

services:
    vaultwarden:
        image: vaultwarden/server:latest
        container_name: vaultwarden
        restart: unless-stopped
        environment:
            DOMAIN: "https://vault.lunarflame.dev"
            ADMIN_TOKEN: ${ADMIN_TOKEN}
            SIGNUPS_ALLOWED: false
        volumes:
            - ./data/:/data/
        ports:
            - "80"
        networks:
            - web
